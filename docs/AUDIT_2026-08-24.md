# TOP-300 Deep Audit — 2026-08-24

The original repository captured the forecasting research phase but not the complete product implied by the conversation.

## Existing scaffold strengths

- clear distinction between trending detection and breakout forecasting
- 19-feature weighted research baseline totaling 100 points
- percent-growth, finite-difference and demand/supply helpers
- methodology, architecture, data-source and roadmap docs
- sample forecast object
- pytest/Ruff CI definition

## Missing product layers

- immutable raw-signal store
- ingestion adapters
- historical replay by cutoff
- feature extraction from observations
- trained horizon models and calibration
- lifecycle/ranking implementation
- walk-forward backtesting
- CLI and demo
- release packaging and conversation checkpoints

## CI defect

The latest scaffold workflow (`32765979589`, commit `628c982`) failed before pytest. Ruff reported import formatting defects plus three `RUF007` findings in `core.py` recommending `itertools.pairwise()`. v1 corrects those defects and extends the scaffold into a runnable pipeline.

## Design corrections

- +300% is a signal, not the forecast.
- personal history is excluded from forecasting.
- historical reproducibility by explicit `as_of` cutoff is the defining invariant.
- demand and supply are forecast separately.
