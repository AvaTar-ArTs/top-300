interface:
  display_name: "Top 300"
  short_description: "Repo-specific patterns and workflows for top-300"
  default_prompt: "Use the top-300 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true